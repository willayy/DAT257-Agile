import React, { useEffect, useRef } from 'react';
import Chart from 'chart.js/auto';
import styles from './lineGraph.module.css';

export default function CardLineChart() {
    React.useEffect(() => {
      var config = {
        // Add valid object assignment here
      };
    });
}
